# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'pastagram' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for pastagram
  # Add Parse pods and install
  pod 'Parse'

  pod "AlamofireImage"
  pod 'MessageInputBar'
   
  target 'pastagramTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'pastagramUITests' do
    # Pods for testing
  end

end
